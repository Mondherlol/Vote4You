{% extends 'base.html.twig' %}

{% block title %}Choix{% endblock %}

{% block body %}
    <h1>Choix</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ choix.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ choix.titre }}</td>
            </tr>
            <tr>
                <th>ImageChoix</th>
                <td>{{ choix.imageChoix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_choix_index') }}">back to list</a>

    <a href="{{ path('app_choix_edit', {'id': choix.id}) }}">edit</a>

    {{ include('choix/_delete_form.html.twig') }}
{% endblock %}
